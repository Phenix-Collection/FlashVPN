<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.polestar.domultiple">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true" android:theme="@style/AppTheme"
        android:name=".PolestarApp">
        <activity
            android:name=".components.ui.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".components.ui.HomeActivity" android:screenOrientation="portrait" />
        <activity android:name=".components.ui.AddCloneActivity" android:screenOrientation="portrait" />
        <activity android:name=".components.ui.NotificationActivity" android:screenOrientation="portrait" />
        <activity android:name=".components.ui.FaqActivity" android:screenOrientation="portrait" />
    </application>

</manifest>
